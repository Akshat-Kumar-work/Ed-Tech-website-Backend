version: '3.8'

services:
  backend:
    build:
      context: .   # Path to directory with Dockerfile
      dockerfile: Dockerfile
    ports:
      - "4000:4000"  # Host:Container
    env_file:
      - .env